namespace SpieleMarmelade.Shared.Stats
{
    // Deliberately small starter set for balancing. Add more values here as new gameplay
    // systems need them - CharacterStats/StatBlockDefinition work with any StatType automatically.
    public enum StatType
    {
        MaxHealth,
        MoveSpeed,
        Damage,
        Armor,
        AttackSpeed,
        JumpForce
    }
}
