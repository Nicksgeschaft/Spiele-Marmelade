namespace SpieleMarmelade.Minigames.JumpBrickScale
{
    // Docs section 3.2: runtimeValue = Clamp((baseValue + Sum(additive)) * Product(multiplicative), min, max).
    public enum StatModifierMode
    {
        Additive,
        Multiplicative,
    }
}
